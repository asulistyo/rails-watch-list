<div class="container mt-5">
  <h1>Listings</h1>
  <%= link_to "Add List", new_list_path, class: "btn btn-primary btn-lg" %>

  <div class="row mt-5">
    <% @lists.each do |list| %>
      <div class="col-4">
        <%= link_to list.name, list %>
      </div>
    <% end %>
  </div>
</div>
